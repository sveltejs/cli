---
"@sveltejs/sv-utils": minor
---

feat: add `@sveltejs/sv-utils/browser`, the environment-agnostic subset (parsers, transforms, language tooling) with no Node builtins, so it can be bundled for the browser
