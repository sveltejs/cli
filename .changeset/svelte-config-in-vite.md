---
'@sveltejs/sv-utils': minor
'sv': minor
---

Add the `svelteConfig` helper and read/edit the svelte config wherever it lives - a `svelte.config.{js,ts}` default export or the object passed to `sveltekit()` in `vite.config.{js,ts}`
