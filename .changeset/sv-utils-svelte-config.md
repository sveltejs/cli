---
'@sveltejs/sv-utils': minor
---

Add `svelteConfig` helper (`find`, `read`, `edit`) to locate and edit the svelte config whether it lives in `svelte.config.{js,ts}` or the `sveltekit()` call in `vite.config.{js,ts}`
