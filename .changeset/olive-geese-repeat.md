---
'@sveltejs/sv-utils': patch
---

feat: add SvelteKit 3 helpers - `isKit3`, `resolveLibPrefix`, `libSubpathImports` and the generated `$app/tsconfig` constants, so add-ons emit the right shape without hardcoding it
