{
	"name": "adder-template-ts",
	"version": "0.0.0",
	"license": "MIT",
	"type": "module",
	"exports": "./dist/index.js",
	"keywords": [
		"sv-adder",
		"sv-add",
		"sv-integration",
		"sv"
	],
	"scripts": {
		"start": "sv add -C temp --community file:../",
		"create-temp": "sv create temp --check-types typescript --template skeleton --no-adders --no-install",
		"build": "tsc",
		"dev": "tsc --watch"
	},
	"dependencies": {
		"@svelte-cli/core": "link:../../packages/core"
	},
	"devDependencies": {
		"sv": "link:../../packages/cli",
		"typescript": "^5.6.2"
	},
	"files": [
		"dist",
		"!dist/**/*.test.*"
	]
}
